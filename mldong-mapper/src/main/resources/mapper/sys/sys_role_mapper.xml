<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mldong.modules.sys.mapper.SysRoleMapper">
	<resultMap id="BaseResultMap" type="com.mldong.modules.sys.entity.SysRole">
		<id column="id" property="id" />
		<result column="create_time" property="createTime" />  
		<result column="update_time" property="updateTime" />  
		<result column="is_deleted" property="isDeleted" />  
		<result column="name" property="name" />  
		<result column="role_key" property="roleKey" />  
		<result column="role_type" property="roleType" />  
		<result column="is_enabled" property="isEnabled" />  
		<result column="remark" property="remark" />  
	</resultMap>
	<delete id="deleteBatchByIds" parameterType="java.util.List">
		delete from sys_role
		where id in
		<foreach collection="list" item="item" index="index" open="("
			close=")" separator=",">
			#{item}
		</foreach>
	</delete>
	<update id="removeBatchByIds" parameterType="java.util.List">
		update sys_role
		<set>
			update_time=now(),
			is_deleted=1
		</set>
		where id in
		<foreach collection="list" item="item" index="index" open="("
			close=")" separator=",">
			#{item}
		</foreach>
	</update>
	<update id="restoreBatchByIds" parameterType="java.util.List">
		update sys_role
		<set>
			update_time=now(),
			is_deleted=0
		</set>
		where id in
		<foreach collection="list" item="item" index="index" open="("
			close=")" separator=",">
			#{item}
		</foreach>
	</update>
</mapper>